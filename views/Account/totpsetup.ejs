<!DOCTYPE html>
<html>
	<head>
		<title><%= title %></title>
		<link rel="stylesheet" href="//netdna.bootstrapcdn.com/bootstrap/3.1.1/css/bootstrap.min.css" />
		<link rel="stylesheet" href="//netdna.bootstrapcdn.com/bootstrap/3.1.1/css/bootstrap-theme.min.css" />
		<link rel='stylesheet' href='/stylesheets/style.css' />
		<script src="//netdna.bootstrapcdn.com/bootstrap/3.1.1/js/bootstrap.min.js"></script>
	</head>
	<body>
		<div class="container">
			<div class="col-sm-6 col-sm-offset-3">
				<h1><span class="fa fa-sign-in"></span> Setup Google authenticator</h1>
				<!-- show any messages that come back with authentication -->
				<% if (message.length > 0) { %>
				<div class="alert alert-danger"><%= message %></div>
				<% } %>
				<!-- LOGIN FORM -->
				<form action="/Account/AddAuthenticator" method="post">
					<input type="hidden" name="_csrf" value= "<%= token %>" />
					<div class="form-group">
						<p>Scan this QR Code in Google Authenticator</p>
						<img src="<%- qrImage %>"/>
						<p>- or enter this key manually -</p>
						<p class="text-danger"><%= key %></p>
					</div>
					<div class="form-group">
						<label>Enter code</label>
						<input type="text" class="form-control" name="code">
					</div>
					<button type="submit" class="btn btn-warning btn-lg">Verify</button>
				</form>
				<hr>
			</div>
		</div>
	</body>
</html>